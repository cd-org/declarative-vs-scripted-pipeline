pipeline {
    agent any

    stages {
        stage('Echo Message') {
            steps {
                echo 'This is executed within a DECLARATIVE Pipeline'
            }
        }
    }
    post {
        always {
            echo 'This will always run'
            sh 'rm -rf *'
        }
    }
}
